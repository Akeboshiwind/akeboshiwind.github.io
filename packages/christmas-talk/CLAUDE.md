# Christmas Talk

Interactive presentation app with real-time audience participation via Ably.

## Commands

```bash
bun install       # Install dependencies
bb build          # Build for local dev
bb build:ci       # Build for production (with base-path)
bb dev            # Dev mode with watch + serve
bb serve          # Serve built files
bb clean          # Clean build artifacts
```

## Tech Stack

- **Squint** - ClojureScript to JavaScript compiler
- **Bun** - JavaScript runtime and bundler
- **Babashka** - Task runner
- **Tailwind CSS** - Styling
- **Ably** - Real-time messaging for audience interaction
- **Reagami** - State management
- **QRCode** - QR code generation for audience join links

## Structure

```
src/
  index.html       # HTML template
  logos/           # Tech logos
  talk/
    app.cljs       # Main entry point
    presenter.cljs # Presenter view
    audience.cljs  # Audience view
    display.cljs   # Display/projector view
    ably.cljs      # Ably real-time integration
    ui.cljs        # Shared UI components
build/             # Compiled JS (generated by squint)
target/public/     # Final build output
```
